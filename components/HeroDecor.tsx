/**
 * Ancien décor parallax (points, anneaux, croix, blobs).
 * Design « entreprise établie » : plus aucun décor abstrait.
 * Le composant est conservé pour ne pas casser les imports,
 * mais ne rend plus rien.
 */
export default function HeroDecor(_props: { dense?: boolean }) {
  return null;
}
